{{- if .Values.userService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.userService.name }}-api-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "user-module.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.userService.name }}
    app.kubernetes.io/config-type: api
data:
  api-exposure.xml: |
    <?xml version="1.0" encoding="UTF-8"?>
    <api-config>
        <exposure>
            <paths>
                {{- range .Values.userService.config.api.authRequiredPaths }}
                <path pattern="{{ . }}" enabled="true" authRequired="true"/>
                {{- end }}
                {{- range .Values.userService.config.api.publicPaths }}
                <path pattern="{{ . }}" enabled="true" authRequired="false"/>
                {{- end }}
            </paths>
            <rate-limit qps="{{ .Values.userService.config.api.rateLimitQps }}" threads="{{ .Values.userService.config.api.rateLimitThreads }}"/>
        </exposure>
        <doc>
            <knife4j enabled="true" title="用户服务API" version="1.0.0"/>
        </doc>
    </api-config>
{{- end }}
