{{- if .Values.userAuthService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.userAuthService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "user-module.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.userAuthService.name }}
spec:
  replicas: {{ .Values.userAuthService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.userAuthService.name }}
      {{- include "user-module.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: {{ .Values.userAuthService.name }}
        {{- include "user-module.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Values.userAuthService.name }}
          image: {{ include "user-module.image" .Values.userAuthService.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.userAuthService.service.targetPort }}
              protocol: TCP
          {{- with .Values.userAuthService.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: app-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/application.xml
              subPath: application.xml
              readOnly: true
            - name: datasource-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/redis.properties
              subPath: redis.properties
              readOnly: true
            - name: log-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/log4j2.xml
              subPath: log4j2.xml
              readOnly: true
            - name: log-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/log.properties
              subPath: log.properties
              readOnly: true
            - name: api-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/api-exposure.xml
              subPath: api-exposure.xml
              readOnly: true
            - name: protection-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/overload-protection.json
              subPath: overload-protection.json
              readOnly: true
            - name: protection-config-volume
              mountPath: {{ .Values.userAuthService.configMountPath }}/auth.properties
              subPath: auth.properties
              readOnly: true
          {{- if .Values.common.healthCheck.enabled }}
          livenessProbe:
            {{- toYaml .Values.common.healthCheck.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.common.healthCheck.readinessProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.userAuthService.resources | nindent 12 }}
      volumes:
        - name: app-config-volume
          configMap:
            name: {{ .Values.userAuthService.name }}-app-config
            defaultMode: 0644
        - name: datasource-config-volume
          configMap:
            name: {{ .Values.userAuthService.name }}-datasource-config
            defaultMode: 0644
        - name: log-config-volume
          configMap:
            name: {{ .Values.userAuthService.name }}-log-config
            defaultMode: 0644
        - name: api-config-volume
          configMap:
            name: {{ .Values.userAuthService.name }}-api-config
            defaultMode: 0644
        - name: protection-config-volume
          configMap:
            name: {{ .Values.userAuthService.name }}-protection-config
            defaultMode: 0644
{{- end }}
